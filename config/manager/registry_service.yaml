apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: system
  labels:
    app.kubernetes.io/name: agentrax
    app.kubernetes.io/component: registry
spec:
  selector:
    control-plane: controller-manager
  ports:
    - name: registry
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
